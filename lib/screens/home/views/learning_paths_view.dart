import 'package:flutter/material.dart';

class LearningPathsView extends StatelessWidget {
  const LearningPathsView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Learning Paths'),
    );
  }
}
